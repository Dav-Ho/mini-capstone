<h2>Here are all of your items</h2>

<% @products.each do |product| %>
      <h3>Title:<%= link_to product.name, "/products/#{product.id}" %> </h3>

  <ol>Price:<%= product.price %></ol>
  <ol>Color:<%= product.color %></ol>
  <ol>Description:<%= product.description %></ol>

<% end %>
